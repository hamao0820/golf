import Column from './column';

class Layout {
    #columns: Column[];
    constructor(columns: Column[]) {
        this.#columns = columns;
    }
    getSize() {
        // TODO
        return;
    }
    takeCard() {
        // TODO
        return;
    }
}

export default Layout;
